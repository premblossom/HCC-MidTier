server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/innoura_hcc
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true